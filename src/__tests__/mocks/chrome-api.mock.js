/**
 * Chrome API centralized mock
 *
 * This file provides standardized mock implementations for Chrome Extension API
 * that can be imported and used across tests.
 *
 * @module chrome-api.mock
 */

// Note: This is an empty template file that will be filled in the next task (T005)
// The implementation will replace Jest mocks with Vitest mocks (vi instead of jest)

// Empty placeholder for now
const chromeMock = {};

export default chromeMock;
